# assets/

This directory stores pre-trained model artifacts:

- `rf_classifier.pkl`   — Trained RandomForest pipeline (StandardScaler + RF)

These files are auto-generated on first run of `main.py` or manually via:

    python train_model.py

They are excluded from version control via `.gitignore`.
